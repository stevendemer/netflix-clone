import PropTypes from "prop-types";

export default function Row({ title, subtitle, imageURL }) {}

Row.propTypes = {
  title: PropTypes.string,
  subtitle: PropTypes.string,
  imageURL: PropTypes.string,
};
