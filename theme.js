import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    red: {
      100: "#E50914",
      200: "#E50914",
      300: "#E50914",
      400: "#B00710",
    },
  },
});

export default theme;
